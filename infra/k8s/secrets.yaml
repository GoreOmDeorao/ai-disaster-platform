# DO NOT commit real values — this is a template only
# Apply manually: kubectl apply -f secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: disaster-secrets
  namespace: disaster-platform
type: Opaque
stringData:
  database-url: "postgres://disaster_user:disaster_pass@postgres-service:5432/disaster_db?sslmode=disable"
  redis-url: "redis://redis-service:6379"
  fcm-server-key: "REPLACE_WITH_ACTUAL_FCM_KEY"
